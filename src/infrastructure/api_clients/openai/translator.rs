use crate::infrastructure::inference::LLMInferenceResult;
use super::dto::{ResponseDTO, RequestDTO};

fn build_request_dto() -> RequestDTO {
    
}

fn build_llm_result(response: ResponseDTO) -> LLMInferenceResult {

}
